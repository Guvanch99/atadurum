const initialState = {}

export const credentialsReducer = (state = initialState, { type, payload }) => {
  switch (type) {
    /*case :
        return { ...state, ...payload }*/

    default:
      return state
  }
}
